spring:
  application:
    name: Backend
  datasource:
    initialization-mode: never
    # url: jdbc:postgresql://10.93.128.209:5432/aquarium_monitoring # Replace with valid Data
    # username: postgres
    # password: Start1234!
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
mqtt:
  broker:
    url: tcp://10.93.128.211:1883
    client:
      id: SpringBootClient
