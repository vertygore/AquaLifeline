spring:
  application:
    name: Backend
  datasource:
      url: jdbc:mysql://localhost:3306/aqualifelinedb
      username: root
      password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
mqtt:
  broker:
    url: tcp://10.93.128.211:1883
    client:
      id: SpringBootClient
